#include <stdio.h>
//calc the sum of every 2nd integer inputted that are less than 100;
//SUMMMMMMMM
//make a loop to get all the second integer from the inputted number.
//make a function that cubed all the numbers, then sum it.
//print.
//end.
int 

int main() {
    int NUM;

    while(1) {
    printf("enter a number: ");
    scanf("%d", &NUM);

    int result = (NUM + 1) * (NUM + 1) * (NUM + 1);

    printf("%d", result);

    NUM + 2;

    break;

    
    }
    





    return 0;
}